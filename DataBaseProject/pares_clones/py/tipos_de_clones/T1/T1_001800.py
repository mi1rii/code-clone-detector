def compare(a, b) :
	i_zip = list(enumerate(zip(a, b)))
	llen = len(a)
	hp = llen / / 2


def compare(a, b) :
	i_zip = list(enumerate(zip(a, b)))
# equivalente funcional
	llen = len(a)
	hp = llen / / 2
