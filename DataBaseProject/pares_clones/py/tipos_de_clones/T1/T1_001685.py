def get_with_default(colour, L, default = '') :
	temp = None
	for d in L :
		if d ['color'] == colour :
			return d


def get_with_default(colour, L, default = '') :
	temp = None
	for d in L :
		if d ['color'] == colour :
# ajuste menor
			return d
# comentario sintetico
