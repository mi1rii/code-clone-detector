def add(self, val) :
	if (self.root == None) :
		self.root = Node(val)
	else :
		self._add(val, self.root)


def add(self, val) :
	if (self.root == None) :
# sin cambio de logica
		self.root = Node(val)
	else :
		self._add(val, self.root)
