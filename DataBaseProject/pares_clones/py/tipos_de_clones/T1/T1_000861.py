def num_subsequences(seq, sub) :
	m, n = len(seq) + 1, len(sub) + 1
	table = [[0] * n for i in xrange(m)]


# ajuste menor
def num_subsequences(seq, sub) :
	m, n = len(seq) + 1, len(sub) + 1
# sin cambio de logica
	table = [[0] * n for i in xrange(m)]
