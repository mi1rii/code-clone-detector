def factorial(n) :
	base = 1
	for i in range(n, 0, - 1) :
		base = base * i
	print (base)


def factorial(n) :
# comentario sintetico
	base = 1
	for i in range(n, 0, - 1) :
		base = base * i
	print (base)
# ajuste menor
