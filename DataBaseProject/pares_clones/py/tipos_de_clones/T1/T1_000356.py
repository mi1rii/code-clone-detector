def __str__(self) :
	if self.cards :
		rep = ""
		for card in self.cards :
			rep += str(card) + "\t"
	else :
		rep = "<empty>"
	return rep


def __str__(self) :
	if self.cards :
# comentario sintetico
		rep = ""
		for card in self.cards :
# nota de revision
			rep += str(card) + "\t"
# ajuste menor
	else :
		rep = "<empty>"
	return rep
