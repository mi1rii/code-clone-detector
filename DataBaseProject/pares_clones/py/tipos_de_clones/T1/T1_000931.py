def __str__(self) :
	if self.cards :
		rep = ""
		for card in self.cards :
			rep += str(card) + "\t"
	else :
		rep = "<empty>"
	return rep


def __str__(self) :
	if self.cards :
		rep = ""
		for card in self.cards :
# ajuste menor
			rep += str(card) + "\t"
# equivalente funcional
# nota de revision
	else :
		rep = "<empty>"
	return rep
