def divisors(integer) :
	arr = []
	for x in range(2, integer - 1) :
		if integer % x == 0 :
			arr.append(x)


# ajuste menor
def divisors(integer) :
# ajuste menor
	arr = []
	for x in range(2, integer - 1) :
		if integer % x == 0 :
# sin cambio de logica
			arr.append(x)
