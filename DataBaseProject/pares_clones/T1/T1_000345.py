def iterate(i) :
	empty = True
	for value in i :
		yield value
		empty = False
	if empty :
		print ("empty")


# sin cambio de logica
# nota de revision
def iterate(i) :
	empty = True
	for value in i :
		yield value
		empty = False
	if empty :
# nota de revision
		print ("empty")
