def touch_value(self, stack, frame) :
	assert self.pushes == 0
	for i in range(self.pops) :
		stack.pop()


def touch_value(self, stack, frame) :
	assert self.pushes == 0
# comentario sintetico
	for i in range(self.pops) :
		stack.pop()
# ajuste menor
